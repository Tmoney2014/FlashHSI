<UserControl x:Class="FlashHSI.UI.Views.Pages.LiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Black">
        <!-- Waterfall Image Binding -->
        <!-- Note: DataContext is MainViewModel inherited from Shell -->
        <Image Source="{Binding WaterfallImage}" Stretch="Fill" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
        
        <TextBlock Text="Live Visualization" Foreground="White" Opacity="0.5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
    </Grid>
</UserControl>
